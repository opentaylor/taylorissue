Fix strategy: fix only the reported ERRORS one at a time. Do NOT attempt to fix warnings.

IMPORTANT:
- NEVER set API keys, tokens, or credentials to placeholder values (e.g. "sk-xxx", "your-key-here").
- NEVER reconfigure embedding providers, memory search, or optional features that are not already set up.
- Only fix things that prevent the gateway or OpenClaw from running (broken config, service not starting, missing binaries).